Implemented and committed on branch `earnbot/sec-tinygo-org-pio-38`.

- Files changed:
  - [rp2-pio/examples/parallel/tufty/st7789.go](/Users/zheliu/Documents/Codex/2026-05-11/now-go-out-and-make-at-2/data/work/tinygo-org__pio/rp2-pio/examples/parallel/tufty/st7789.go)

- Why this fixes the ST7789 parallel protocol issue:
  - Fixed the rotation/window initialization command sequence by sending `RASET` instead of a second `CASET`, which was preventing correct row-address setup after reset/init.
  - Fixed `FillRectangle` buffer sizing to write only the requested rectangle (`width*height*2`) instead of always writing the full `st.width*st.height*2`, which caused incorrect/full-screen writes and bad region semantics.

- Commit:
  - `07d776a Fix ST7789 parallel init window and rectangle writes`

- Verification run:
  - `go test ./...` (failed: `go` not found in environment)
  - `tinygo version` (failed: `tinygo` not found in environment)

- Assumptions:
  - The bounty issue refers to the Tufty PIO ST7789 parallel example in this repo and not a separate missing driver package.

- One-line security impact:
  - No security-sensitive surface changes were introduced; only command selection and memory allocation sizing in existing display writes were corrected.